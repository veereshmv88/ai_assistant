# ═══════════════════════════════════════════════════════════════════════
# AI Blind Assistant — Python Dependencies
# ═══════════════════════════════════════════════════════════════════════
# Install: pip install -r requirements.txt
# Raspberry Pi notes:
#   - RPi.GPIO and picamera2 only available on Raspberry Pi OS
#   - Set MOCK_SENSORS=True in config for dev on Windows/macOS
# ═══════════════════════════════════════════════════════════════════════

# ── Core async & system ───────────────────────────────────────────────
asyncio-mqtt>=0.16.1
aiofiles>=23.2.1
aiohttp>=3.9.3

# ── Configuration & utilities ─────────────────────────────────────────
pyyaml>=6.0.1
python-dotenv>=1.0.1
rich>=13.7.0
click>=8.1.7

# ── Computer Vision ───────────────────────────────────────────────────
opencv-python-headless>=4.9.0.80
ultralytics>=8.2.0           # YOLOv8 / YOLO-World
Pillow>=10.3.0
numpy>=1.26.4
scipy>=1.13.0

# ── Florence-2 / Transformers (scene understanding) ───────────────────
transformers>=4.40.0
torch>=2.2.0                 # CPU build on Pi; use torch==2.2.0+cpu
torchvision>=0.17.0
timm>=0.9.16
einops>=0.7.0
flash-attn>=2.5.0 ; sys_platform != "linux" or platform_machine != "aarch64"

# ── Ollama (local LLM / vision) ───────────────────────────────────────
ollama>=0.2.0

# ── Speech-to-Text ────────────────────────────────────────────────────
openai-whisper>=20231117
faster-whisper>=1.0.1        # quantized Whisper for Pi (preferred)
vosk>=0.3.45                 # streaming STT hotword detection
pyaudio>=0.2.14
sounddevice>=0.4.6
soundfile>=0.12.1

# ── Text-to-Speech ────────────────────────────────────────────────────
piper-tts>=1.2.0             # pip install piper-tts
# espeak-ng is installed via apt: sudo apt install espeak-ng

# ── OCR ───────────────────────────────────────────────────────────────
easyocr>=1.7.1
pytesseract>=0.3.10          # fallback OCR

# ── Face Recognition ──────────────────────────────────────────────────
face-recognition>=1.3.0      # requires cmake & dlib
# dlib is auto-pulled by face-recognition


# ── Navigation / Maps ────────────────────────────────────────────────
requests>=2.31.0
osmnx>=1.9.1                 # OpenStreetMap graph (offline routing)
networkx>=3.3

# ── Emergency SOS ────────────────────────────────────────────────────
twilio>=9.1.0

# ── Database (scene memory) ───────────────────────────────────────────
# sqlite3 is part of Python stdlib — no extra install needed

# ── Raspberry Pi Hardware (Pi only) ──────────────────────────────────
# Uncomment when deploying to Raspberry Pi:
# RPi.GPIO>=0.7.1
# picamera2>=0.3.19

# ── Testing & dev utilities ───────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.6
